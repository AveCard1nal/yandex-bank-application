apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cash-service.fullname" . }}-test"
  labels:
    app.kubernetes.io/name: {{ include "cash-service.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: curlimages/curl:8.10.1
      args: ["curl", "-f", "http://{{ include "cash-service.fullname" . }}:80/actuator/health"]
             restartPolicy: Never
