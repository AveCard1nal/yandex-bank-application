apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "transfer-service.fullname" . }}-test"
  labels:
    app.kubernetes.io/name: {{ include "transfer-service.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: curlimages/curl:8.10.1
      args: ["curl", "-f", "http://{{ include "transfer-service.fullname" . }}:80/actuator/health"]
             restartPolicy: Never
